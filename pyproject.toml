[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "claude-harness"
version = "0.1.0"
description = "Portable harness engineering toolkit for Claude Code"
requires-python = ">=3.10"
license = "MIT"

[project.scripts]
harness = "harness.cli:main"
